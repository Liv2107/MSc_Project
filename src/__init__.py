"""Research package for the AI-image-detector generalisation study.

WHY THIS FILE EXISTS
--------------------
It marks ``src`` as an importable package and provides one place for package-level
metadata. It deliberately imports no heavyweight libraries or experiment modules:
package import should never initialise a GPU, load a model, or read a dataset.

IMPLEMENTATION CHECKLIST
------------------------
[ ] Add a package version once releases are tagged.
[ ] Keep imports side-effect free.
[ ] Do not expose unstable internal functions merely for shorter imports.
"""
