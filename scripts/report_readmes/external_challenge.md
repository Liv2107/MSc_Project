# External contemporary-generator challenge — exports

Regenerated by `python -m scripts.build_external_figures`. Derived from the external run
directory and the internal runs it compares against; safe to delete.

**Route B. The generator is `astra_mediated_unidentified` and cannot be named.** No figure
or number here supports an architectural claim. The evaluation set is 200 images (100
generated + 100 authentic), the pre-registered *minimum reportable* tier — not the
recommended 250-per-class tier that would match the internal unseen test's resolution.

| file | contents |
|---|---|
| `fig15_external_vs_internal_average_precision.{pdf,png}` | PR-AUC for every frozen detector on its internal in-distribution set, its internal unseen generator, and the external set. **The primary discrimination figure.** |
| `fig15_external_vs_internal_roc_auc.{pdf,png}` | The same comparison on ROC-AUC. |
| `fig15_external_vs_internal_f1.{pdf,png}` | The same comparison on F1 at the fixed 0.5 threshold. |
| `fig16_external_roc_pr.{pdf,png}` | ROC and precision-recall curves for the primary frozen detector: external beside its own internal unseen and in-distribution partitions, so the *shape* of the difference is visible and not only its area. Drawn from the per-sample prediction files. |
| `fig17_external_score_distributions.{pdf,png}` | Where authentic and generated scores fall relative to the fixed 0.5 operating point, per detector. This is what determines the reported F1. |
| `tab04_external_challenge.{csv,md}` | The external result as one flat table, with each detector's saved internal numbers and the external-minus-internal differences alongside. |

A detector with no number for an evaluation set is annotated `not measured` rather than
drawn as a zero bar: the all-seven-generators baseline has no held-out generator, so it has
no unseen row.

## The reading these figures support

The external set is **not** harder than the held-out internal generator; it is roughly as
easy as in-distribution data. What that supports is the narrow claim that **degradation on
an unseen generator is generator-specific, not a function of how recent the generator is** —
VQDM, a 2023 model *inside* the benchmark, defeats this detector far more thoroughly than a
2026 assistant-mediated image tool does.

It does **not** support "the detector generalises to contemporary generators", and it
carries two uncontrolled confounds that could explain the external number without any
generator artefact: the generated images were produced at 1254px and downscaled far more
than any internal image, and the prompt set yields a cleaner photographic style than the
ImageNet-derived authentic pool. Section 15.0.1 of
`outputs/report/dissertation_results/RESULTS_NOTES.md` states this in full, and it must
appear wherever the external number appears.

Step 7 of the protocol — limited-data recovery against the external set — was not run. The
recovery and depth-ablation cells retained checkpoint digests only, not weights, so no
adapted detector exists to score without re-training.
