# In-distribution GenImage baseline using all eight official generator subsets.
#
# REQUIRES THE FULL OFFICIAL RELEASE. The Tiny GenImage subset on this machine has only
# seven generators (no Stable Diffusion v1.4), so this config will fail against it and
# v1.4 is deliberately NOT substituted. For Tiny GenImage use
# configs/tiny_genimage_baseline.yaml instead.
# The importer maps official train -> train and partitions official val into
# model-selection validation plus untouched test data.

base_config: base.yaml

experiment:
  type: baseline
  name: genimage_all_generators_baseline

data:
  root: data
  manifest_path: data/manifests/genimage.csv
  split_path: data/manifests/genimage_splits.csv

generators:
  train:
    - midjourney
    - stable_diffusion_v1_4
    - stable_diffusion_v1_5
    - adm
    - glide
    - wukong
    - vqdm
    - biggan
  validation:
    - midjourney
    - stable_diffusion_v1_4
    - stable_diffusion_v1_5
    - adm
    - glide
    - wukong
    - vqdm
    - biggan
  test:
    - midjourney
    - stable_diffusion_v1_4
    - stable_diffusion_v1_5
    - adm
    - glide
    - wukong
    - vqdm
    - biggan
  unseen: null
  include_real_images: true

training:
  fine_tune_mode: head_only
